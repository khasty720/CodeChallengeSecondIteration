<% if @pattern.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      <%= pluralize(@pattern.errors.count, "error") %> prohibited this user from being saved:
    </div>

    <ul>
    <% @pattern.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
